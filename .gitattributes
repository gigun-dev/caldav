# iCalendar フィクスチャは CRLF 改行・75 オクテット折り畳みを「オクテット単位で」保持する
# 必要がある(ロスレス往復テストのオクテット等価検証がバイト一致を要求するため)。
# git の自動改行変換(autocrlf/eol)が CRLF↔LF を書き換えると、
#   - オクテット等価テストが環境依存で落ちる
#   - lf-only.ics(意図的に LF のみで作った寛容受理テスト)が CRLF に化ける
# ので、-text でテキスト正規化を完全に無効化し、コミットされたバイト列をそのまま維持する。
*.ics -text
